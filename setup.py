from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An ML project to detect emotions from text',
    author='himanshu',
    license='',
)
